require 'test_helper'

class UsersControllerTest < ActionDispatch::IntegrationTest
  # describe 'POST #create' do
  #   context 'when username is already being used' do
  #     it 'should render the page with error' do
  #       user = create(:user)
  #       post :create, user: { username: user.username, password: user.password }
  #       expect(response).to render_template(:new)
  #       expect(flash[:notice]).to match(/^Sorry! Username is already taken. Please try again./)
  #     end
  #   end
  # end

end # end of UsersControllerTest
